using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleSpawn : MonoBehaviour
{
    public GameObject player;
    void Start()
    {
        //var cloudObject = Instantiate(TriggerControl.instance.cloud, player.transform.position, TriggerControl.instance.cloud.transform.rotation);
        //TriggerControl.instance.cloudClone = cloudObject;
        //TriggerControl.instance.cloudClone.transform.parent = player.transform;
    }
    void Update()
    {
        
    }
}
